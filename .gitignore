# Local assistant config
.claude/
CLAUDE.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# OS
.DS_Store

# Local data and logs
data/
logs/
*.log

# One-off analyses and probes (readable locally, never published)
scratch/
ros/pepin_src/
ros/maps/rec/
ros/maps/*.posegraph
ros/maps/*.data
ros/maps/*_throttle*
.coverage
ros/maps/rtabmap*.db*
# the board's mode, recorded by ros/laptop.sh start so ros/laptop.sh vslam need not ask
ros/.mode
ros/maps/last_pose.json

# written by the depth node at run time (the persisted affine law)
ros/maps/depth_law.json
