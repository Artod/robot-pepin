{
  "neck": {
    "id": 9,
    "center": 2048,
    "min": 257,
    "max": 3812
  },
  "head": {
    "id": 10,
    "center": 2048,
    "min": 1814,
    "max": 3090
  },
  "reference": {
    "pan_ticks": 2021,
    "tilt_ticks": 2311,
    "x_m": 0.0,
    "y_m": 0.0,
    "z_m": 1.203,
    "axis_z_m": 1.134,
    "pitch_deg": 23.8,
    "pan_sign": -1,
    "tilt_sign": 1,
    "signs_verified": true,
    "mount_measured": "2026-09-12",
    "ticks_note": "x_m/y_m/z_m/pitch_deg are config/camera.json's mount, and both are MEASURED, not fitted (2026-09-12): the tape reads floor to tilt axis 1.134 m (axis_z_m, a documented number nothing reads) and lens to axis 0.086 m up, 0.025 m forward, which is the pivot block below; the pitch comes from the encoder and four still frames of one room — head level by eye the tilt servo reads 2068 ticks and the picture is 1.0 deg down (+-1.5), and this reference sits 243 ticks further down, so 23.8 deg. The tilt scale those frames verify is the plain 360/4096 per tick (1.07 true degrees per commanded degree, 1.03..1.09 over every variant): pepin.neck.RAD_PER_TICK needs no correction and there is no scale field here (scratch/neck_tilt_scale.txt). The 26.0 deg / 1.23 m these replace came from the lidar-beam fit of 2026-09-10 (scratch/depth_fit_models.py), which was owed a repeat anyway. pan_ticks and tilt_ticks are what the two encoders read at that pose: 2021 / 2311, read 2026-09-11 through the base server with the head untouched since the mount was measured (50 reads, min = max, scratch/neck_ticks_probe.py). Were they null, while they are null the model ignores the encoders and answers the static mount for every reading (pepin.neck.joint_angles), so /neck/state and the neck_tf transform stand still while the head moves. To fill them, leave the head at rest, deploy the base server that answers the neck command (board/README.md) and ask it on its JSON-lines port: { printf '{\"cmd\":\"neck\"}\\n'; sleep 2; } | nc 10.0.0.187 3336 | grep -m1 '\"type\":\"neck\"' — the reply carries pan_ticks and tilt_ticks (the port also broadcasts state lines, hence the grep). scratch/neck_ticks_probe.py --host 10.0.0.187 --n 50 does the same 50 times and prints median/min/max and what the read costs the bus.",
    "signs_note": "pan_sign and tilt_sign say which way an increasing tick count turns the camera. VERIFIED 2026-09-11 by hand with the servos unpowered, watching /neck/state (scratch/neck_signs.csv): the head pushed down took head_tilt from 26 to 103 deg (tilt_sign +1 holds), the head turned to the robot's left took neck_pan from 0 to -124 deg with pan_sign +1, hence pan_sign -1. The check, for a re-assembled neck, in daylight and after the reference ticks are filled in: ros/feature.sh neck on, watch ros2 topic echo /neck/state (or the node's report line), and move the head BY HAND — the base server torques the wheels only (pepin.base.DiffDriveBase.enable), so the neck servos are free to push. Tilting the head down, the picture tilting down with it, must make head_tilt GROW; if it shrinks, tilt_sign is -1. Panning the head to the robot's left (+y of base_link) must make neck_pan GROW; if it shrinks, pan_sign is -1. Then set signs_verified true. The powered version of the same check is scripts/jog.py neck --id 10 --delta 100, which needs the servo bus on the USB of the machine running it and pepin-base stopped on the board (board/README.md) — on the assembled robot the hand check is the cheap one.",
    "pivot_note": "the lever arms below: camera_from_tilt_* are the tape of 2026-09-12 (the lens sits 0.086 m above the tilt axis and 0.025 m in front of it, in the head's own frame), so the camera now swings on a real arm as the head tilts instead of turning about its own point. tilt_from_pan_* are still zero and still unmeasured: the pan never moved in any of the measurements, so nothing constrains them. Measure with a ruler, neck straight: pan axis to tilt axis, forward and up."
  },
  "pivot": {
    "tilt_from_pan_x_m": 0.0,
    "tilt_from_pan_z_m": 0.0,
    "camera_from_tilt_x_m": 0.025,
    "camera_from_tilt_z_m": 0.086
  }
}
