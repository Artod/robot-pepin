{
  "active": "stereo",
  "overview": {
    "stream": "http://{board}:8080/stream",
    "width": 1280,
    "height": 720,
    "hfov_deg": 82.94,
    "calibrated": true,
    "mount": {
      "x_m": 0.0,
      "y_m": 0.0,
      "z_m": 1.203,
      "pitch_deg": 23.8,
      "note": "MEASURED, not fitted. pitch_deg 23.8 comes from the encoder and the level frames: with the head level by eye the tilt servo reads 2068 ticks and the picture is 1.0 deg down (+-1.5), and the reference pose sits 2311 - 2068 = 243 ticks further down at 360/4096 deg a tick, which is 23.8 (scratch/neck_tilt_scale.txt, four still frames of one room, 2026-09-12). z_m 1.203 is the tape: floor to tilt axis 1.134 m (reference.axis_z_m in config/neck.json) plus the lens 0.086 m above it and 0.025 m in front, turned down by that pitch. Both replace the 26.0 / 1.23 the lidar-beam fit of 2026-09-10 voted for (scratch/depth_fit_models.py), which was owed a repeat anyway: its beams were projected at a lidar mount the tape corrected on 2026-09-12."
    },
    "frames": {
      "link": "camera_link",
      "optical": "camera_optical"
    },
    "intrinsics": {
      "width": 1280,
      "height": 720,
      "hfov_deg": 82.94,
      "fx": 724.127,
      "fy": 726.828,
      "cx": 652.04,
      "cy": 373.983,
      "dist": [
        -0.150009,
        -0.129449,
        -0.002416,
        -0.001635,
        0.091751
      ],
      "model": "plumb_bob",
      "rms": 0.2304,
      "views": 45,
      "board": "9x6 inner corners, 24.5 mm squares",
      "date": "2026-09-13"
    }
  },
  "stereo": {
    "stream": "http://{board}:8080/stream",
    "width": 800,
    "height": 600,
    "hfov_deg": 94.0,
    "rig": {
      "layout": "side_by_side",
      "frame_width": 1600,
      "frame_height": 600,
      "upside_down": true,
      "baseline_m_nominal": 0.063,
      "calibration": "stereo_calibration.json"
    },
    "mount": {
      "x_m": 0.0,
      "y_m": 0.0,
      "z_m": 1.203,
      "pitch_deg": 23.8,
      "note": "The NECK's link, exactly the overview camera's measured mount (z_m 1.203, pitch_deg 23.8: the tape and the encoder, 2026-09-12), because the module is taped onto that webcam and the board publishes base_link -> camera_link from the neck's encoders either way. Where the module's LEFT eye sits and looks relative to that link is the 'eye' block below — never folded into this one, so the neck's frame means the same thing under every head."
    },
    "eye": {
      "x_m": -0.011,
      "y_m": 0.0305,
      "z_m": 0.025,
      "roll_deg": -3.7,
      "pitch_deg": -5.6,
      "yaw_deg": 4.3,
      "note": "The rectified LEFT eye in camera_link's axes (x forward, y left, z up), composed into camera_link -> camera_optical. MEASURED 2026-09-21 with the robot parked square to a door, at 2 m and again at 3 m: the floor's plane in the stereo depth gives the eye's pitch, roll and height (18.0-18.8 deg down where the link says 24.06; 1.227-1.233 m where the link says 1.203), the door's plane against the lidar's line through the same door gives its yaw (5.3-5.7 deg left where the neck's pan says 0.97). The two scenes agree to 0.7 deg and these are their mean. The module is taped, so a knock changes these: the depth node's law line (a 1.00) is the alarm. y_m is NOT measured: half the calibrated 61 mm baseline to the left, assuming the module is centred on the webcam's lens."
    },
    "frames": {
      "link": "camera_link",
      "optical": "camera_optical"
    },
    "notes": "The stereo head: ONE side-by-side MJPEG frame of 1600x600 from ustreamer on the board (rig.frame_width x rig.frame_height), cut into two 800x600 eyes by pepin.stereo.SideBySide. width/height are ONE EYE — the picture every consumer of /camera/image sees — while the frame on the wire is twice as wide. upside_down is MEASURED on a real frame: the module is taped upside down, so each half is turned 180 deg and the halves trade places, and only then is the disparity of near objects positive. hfov_deg 94.0 and baseline_m_nominal 0.063 are the VENDOR's numbers, the nominal pinhole pepin.camera.optics falls back to while there is no calibration; both are replaced by config/stereo_calibration.json the moment it exists (the rectified pinhole and the baseline come from there, never from here). This block has no intrinsics of its own: calibrated means that file exists and loads."
  },
  "notes": "The lens is measured: ros/calibrate.sh (45 views of a 9x6 checkerboard, rms 0.23 px, 2026-09-13) wrote the intrinsics block and set calibrated: true, and pepin.camera.optics answers from it. hfov_deg is the nominal pinhole that reader falls back to when calibrated is false — it is 82.94, the field of view the calibration measured, because the best known number is what a bad calibration must be switched off onto; it was 78.0, the lidar-beam fit of 2026-09-10, and 70 is the vendor number that put the image edges 8 deg wrong. The mount beside it is measured too: the pitch from the level frames and the encoder, the height from a tape (see the mount note). The 78 / 26 self-calibration is STALE and is not the source of any number in this file any more. WHICH camera the robot's head is, is the top-level \"active\" above: every node that reads this file asks pepin.camera for the active one, and PEPIN_CAMERA in a node's environment overrides it (ros/laptop.sh forwards it into the container). The board's own half of the switch is /etc/default/pepin-camera (board/pepin-camera.service): the device and the resolution ustreamer serves. The two must agree — \"active\": \"stereo\" beside a board still streaming 1280x720 mono is a node splitting a webcam picture down the middle."
}
