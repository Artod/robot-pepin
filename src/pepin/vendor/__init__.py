"""Third-party source vendored into this repo, each subpackage with its upstream and licence.

A vendored package is not this project's code: it is copied verbatim but for the edits its own
``__init__`` lists, and it is excluded from ruff and mypy (pyproject.toml) so that a diff against
upstream stays the only thing that matters about it.
"""
